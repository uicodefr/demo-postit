info:
  name: List Boards
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/postit/boards
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
